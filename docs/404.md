---
layout: page
---

<script setup>
import { ref, onMounted } from 'vue'

const lang = ref('en')

onMounted(() => {
  lang.value = window.location.pathname.startsWith('/AI-Practices/zh') ? 'zh' : 'en'
})
</script>

<div class="not-found">
  <p class="code">404</p>
  <h1 class="title">{{ lang === 'zh' ? '页面未找到' : 'Page Not Found' }}</h1>
  <p class="desc">{{ lang === 'zh' ? '抱歉，您访问的页面不存在。' : 'Sorry, the page you are looking for does not exist.' }}</p>
  <a :href="lang === 'zh' ? '/AI-Practices/zh/' : '/AI-Practices/en/'" class="link">
    {{ lang === 'zh' ? '返回首页' : 'Go Home' }}
  </a>
</div>

<style scoped>
.not-found {
  padding: 64px 24px;
  text-align: center;
}
.code {
  font-size: 64px;
  font-weight: 700;
  color: var(--vp-c-brand);
}
.title {
  font-size: 24px;
  margin: 16px 0;
}
.desc {
  color: var(--vp-c-text-2);
  margin-bottom: 24px;
}
.link {
  display: inline-block;
  padding: 8px 24px;
  border-radius: 20px;
  background: var(--vp-c-brand);
  color: white;
  text-decoration: none;
  transition: opacity 0.2s;
}
.link:hover {
  opacity: 0.8;
}
</style>
