# DCGAN图像生成

**难度**: ⭐⭐⭐⭐☆ (高级)

使用深度卷积生成对抗网络(DCGAN)生成逼真的图像，学习GAN的核心原理和训练技巧。

## 📋 项目概述

### 项目目标
构建一个DCGAN模型，能够从随机噪声生成逼真的图像（人脸、手写数字等）。

### GAN原理
生成对抗网络由两个神经网络组成：
- **生成器(Generator)**: 从随机噪声生成图像
- **判别器(Discriminator)**: 判断图像是真实还是生成的

两个网络相互对抗，最终生成器学会生成逼真的图像。

### 数据集
- **CelebA**: 20万张名人人脸图像
- **MNIST**: 手写数字图像
- **CIFAR-10**: 自然图像

## 🚀 快速开始

### 1. 安装依赖

```bash
pip install -r requirements.txt
```

### 2. 下载数据

```bash
python src/download_data.py --dataset celeba
```

### 3. 训练DCGAN

```bash
# 训练MNIST DCGAN
python src/train.py --dataset mnist --epochs 50

# 训练CelebA DCGAN
python src/train.py --dataset celeba --epochs 100

# 使用GPU加速
python src/train.py --dataset celeba --epochs 100 --gpu
```

### 4. 生成图像

```python
from src.model import DCGAN
import numpy as np

# 加载模型
gan = DCGAN()
gan.load_generator('models/generator_final.h5')

# 生成图像
noise = np.random.normal(0, 1, (16, 100))
generated_images = gan.generate(noise)

# 保存图像
gan.save_images(generated_images, 'results/generated.png')
```

## 📊 项目结构

```
17_DCGAN图像生成/
├── README.md
├── requirements.txt
├── data/
│   ├── raw/
│   └── processed/
├── notebooks/
│   ├── 01_GAN基础.ipynb
│   ├── 02_DCGAN实现.ipynb
│   ├── 03_训练技巧.ipynb
│   └── 04_图像生成.ipynb
├── src/
│   ├── __init__.py
│   ├── download_data.py  # 数据下载
│   ├── data.py          # 数据加载
│   ├── generator.py     # 生成器
│   ├── discriminator.py # 判别器
│   ├── model.py         # DCGAN模型
│   ├── train.py         # 训练脚本
│   └── generate.py      # 生成脚本
├── models/
│   ├── generator_final.h5
│   └── discriminator_final.h5
└── results/
    ├── training_progress/  # 训练过程图像
    ├── generated_samples.png
    └── loss_curves.png
```

## 🛠️ 模型架构

### 生成器 (Generator)
```python
输入: 100维随机噪声
↓
Dense(7×7×256) + BatchNorm + ReLU
↓
Reshape(7, 7, 256)
↓
Conv2DTranspose(128) + BatchNorm + ReLU  # 14×14
↓
Conv2DTranspose(64) + BatchNorm + ReLU   # 28×28
↓
Conv2DTranspose(3) + Tanh                # 28×28×3
↓
输出: 生成的图像
```

### 判别器 (Discriminator)
```python
输入: 28×28×3图像
↓
Conv2D(64) + LeakyReLU + Dropout
↓
Conv2D(128) + BatchNorm + LeakyReLU + Dropout
↓
Conv2D(256) + BatchNorm + LeakyReLU + Dropout
↓
Flatten + Dense(1) + Sigmoid
↓
输出: 真实概率 [0, 1]
```

## 📈 训练过程

### 训练步骤
1. **训练判别器**
   - 真实图像标记为1
   - 生成图像标记为0
   - 最小化二元交叉熵损失

2. **训练生成器**
   - 生成图像标记为1（欺骗判别器）
   - 最小化判别器的损失

3. **交替训练**
   - 每个batch交替训练D和G
   - 保持两者平衡

### 训练技巧
1. **标签平滑**: 真实标签使用0.9而非1.0
2. **噪声注入**: 向真实图像添加少量噪声
3. **学习率**: 使用较小的学习率(0.0002)
4. **优化器**: Adam优化器，β1=0.5
5. **批标准化**: 稳定训练过程
6. **LeakyReLU**: 避免梯度消失

### 训练监控
```
Epoch 1/100
  D loss: 0.6931, D acc: 50.0%
  G loss: 0.6931

Epoch 50/100
  D loss: 0.3245, D acc: 85.2%
  G loss: 1.8765

Epoch 100/100
  D loss: 0.2891, D acc: 88.5%
  G loss: 2.1234
```

## 💡 项目亮点

1. **完整的GAN实现**: 从零实现DCGAN
2. **训练可视化**: 实时查看生成效果
3. **多数据集支持**: MNIST、CelebA、CIFAR-10
4. **训练技巧**: 解决模式崩溃等问题
5. **图像插值**: 在潜在空间中插值

## 📚 学习要点

### GAN基础
- 生成器和判别器的对抗训练
- 纳什均衡
- 损失函数设计

### DCGAN特点
- 使用卷积层替代全连接层
- 批标准化的重要性
- 转置卷积上采样

### 训练技巧
- 解决模式崩溃
- 平衡G和D的训练
- 超参数调优

### 常见问题
- 模式崩溃 (Mode Collapse)
- 训练不稳定
- 梯度消失/爆炸

## 🎯 改进方向

1. **高级GAN架构**
   - WGAN (Wasserstein GAN)
   - StyleGAN
   - Progressive GAN

2. **条件生成**
   - Conditional GAN
   - 控制生成内容

3. **应用扩展**
   - 图像超分辨率
   - 图像修复
   - 风格迁移

4. **训练优化**
   - 谱归一化
   - 自注意力机制
   - 渐进式训练

## 🔗 参考资源

### 论文
- [DCGAN论文](https://arxiv.org/abs/1511.06434)
- [GAN原始论文](https://arxiv.org/abs/1406.2661)
- [Improved Techniques for Training GANs](https://arxiv.org/abs/1606.03498)

### 教程
- [TensorFlow DCGAN教程](https://www.tensorflow.org/tutorials/generative/dcgan)
- [PyTorch DCGAN教程](https://pytorch.org/tutorials/beginner/dcgan_faces_tutorial.html)

### 代码实现
- [PyTorch-GAN](https://github.com/eriklindernoren/PyTorch-GAN)
- [Keras-GAN](https://github.com/eriklindernoren/Keras-GAN)

## 🎨 生成样例

### MNIST手写数字
训练50轮后可以生成清晰的手写数字。

### CelebA人脸
训练100轮后可以生成逼真的人脸图像。

### 潜在空间插值
在两个随机向量之间插值，生成平滑过渡的图像序列。

---

**开始你的生成式AI之旅吧！** 🎨
